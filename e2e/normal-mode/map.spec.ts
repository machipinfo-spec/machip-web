import { test, expect } from "@playwright/test";

test.describe("Normal Mode - Map", () => {
  // TODO: Implement authentication setup

  test("should allow pin creation", async ({ page }) => {
    // Placeholder
    // 1. Navigate to /map
    // 2. Click on map
    // 3. Verify PinCreationDialog is visible
  });
});
