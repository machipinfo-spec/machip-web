import { test, expect } from "@playwright/test";

test.describe("Normal Mode - Calendar", () => {
  // TODO: Implement authentication setup

  test("should allow actions on calendar", async ({ page }) => {
    // Placeholder
    // 1. Navigate to /calender
    // 2. Select date
    // 3. Verify posts and ability to reply?
  });
});
